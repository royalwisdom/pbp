https://launchschool.com/blog/how-to-use-devise-in-rails-for-authentication

//add to gemfile
gem 'devise'

//run in command
1. bundle install
2. rails generate devise:install
3. rails generate devise overlord
4. rails generate devise:views overlords //creates a bunch of views in the appropriate folder app/views/overlords

//in config/environments/development.rb
config.action_mailer.default_url_options = { host: 'localhost', port: 3000 }

//run in command
rails db:migrate

//add to ApplicationController
 before_action :authenticate_user, if: :devise
 
 
 if overlord_signed_in?
  redirect_to 'overlord/index'
 else
  flash[:error]
 end
 
//sessions controller needs to be created
rails generate controller Sessions create destroy

//devise reference
authenticate_user! - class method (controller only), ensures a logged in user is available to all, or a specified set of controller actions. 
current_user
user_signed_in?
sign_in(@user)
sign_out(@user)
user_session
